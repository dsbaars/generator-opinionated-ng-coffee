language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ng-bootstrap-less' ]; then cd .. && eval "mv $currentfolder generator-ng-bootstrap-less" && cd generator-ng-bootstrap-less; fi

